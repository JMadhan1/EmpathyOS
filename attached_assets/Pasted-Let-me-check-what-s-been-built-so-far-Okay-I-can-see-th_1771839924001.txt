Let me check what's been built so far. Okay I can see the problem clearly. The app is basically just a static landing page — no actual working steps, no API calls, no real functionality. Here's what's wrong and what you need to do:

What's Missing / Broken
No Step 1–4 flow — clicking "Start a Conversation" likely does nothing or goes nowhere functional
No Flask backend routes working (/api/clarify, /api/draft, etc.)
No OpenAI API key connected
No real UI — just a plain landing page with no cards, no inputs, no coaching output
Design is completely basic — none of the warm premium design from the prompt
What To Do Right Now in Replit

Go back to Replit and give it this fix prompt — this is more specific and forceful than the first one:

The current app is broken — it's just a static landing page with no working functionality. 
Rebuild the entire app from scratch properly. Do NOT keep any of the current code.

=== WHAT NEEDS TO BE BUILT ===

A fully working single-page web app called EmpathyOS with:
- A Python Flask backend (app.py) with 4 working API routes
- A single index.html with all 4 steps shown/hidden via JavaScript
- All API calls made via fetch() from script.js to Flask backend
- OpenAI GPT-4o used in every backend route

=== FILE STRUCTURE ===
app.py
prompts.py
requirements.txt
.env
templates/index.html
static/style.css
static/script.js

=== app.py ===

from flask import Flask, render_template, request, jsonify, session
from openai import OpenAI
import os
from dotenv import load_dotenv
import prompts
import json

load_dotenv()
app = Flask(__name__)
app.secret_key = os.urandom(24)
client = OpenAI(api_key=os.getenv("OPENAI_API_KEY"))

@app.route('/')
def index():
    return render_template('index.html')

@app.route('/api/clarify', methods=['POST'])
def clarify():
    data = request.json
    session['step1'] = data
    prompt = prompts.clarify_prompt(data)
    response = client.chat.completions.create(
        model="gpt-4o",
        messages=[
            {"role": "system", "content": prompts.SYSTEM_PROMPT},
            {"role": "user", "content": prompt}
        ],
        response_format={"type": "json_object"}
    )
    result = json.loads(response.choices[0].message.content)
    session['clarify_result'] = result
    return jsonify(result)

@app.route('/api/draft', methods=['POST'])
def draft():
    data = request.json
    data['context'] = session.get('step1', {})
    session['step2'] = data
    prompt = prompts.draft_prompt(data)
    response = client.chat.completions.create(
        model="gpt-4o",
        messages=[
            {"role": "system", "content": prompts.SYSTEM_PROMPT},
            {"role": "user", "content": prompt}
        ],
        response_format={"type": "json_object"}
    )
    result = json.loads(response.choices[0].message.content)
    session['draft_result'] = result
    return jsonify(result)

@app.route('/api/anticipate', methods=['POST'])
def anticipate():
    context = {
        'step1': session.get('step1', {}),
        'step2': session.get('step2', {}),
        'draft_result': session.get('draft_result', {})
    }
    prompt = prompts.anticipate_prompt(context)
    response = client.chat.completions.create(
        model="gpt-4o",
        messages=[
            {"role": "system", "content": prompts.SYSTEM_PROMPT},
            {"role": "user", "content": prompt}
        ],
        response_format={"type": "json_object"}
    )
    result = json.loads(response.choices[0].message.content)
    return jsonify(result)

@app.route('/api/reflect', methods=['POST'])
def reflect():
    data = request.json
    data['context'] = {
        'step1': session.get('step1', {}),
        'step2': session.get('step2', {})
    }
    prompt = prompts.reflect_prompt(data)
    response = client.chat.completions.create(
        model="gpt-4o",
        messages=[
            {"role": "system", "content": prompts.SYSTEM_PROMPT},
            {"role": "user", "content": prompt}
        ],
        response_format={"type": "json_object"}
    )
    result = json.loads(response.choices[0].message.content)
    return jsonify(result)

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=5000, debug=True)

=== prompts.py ===

SYSTEM_PROMPT = """
You are EmpathyOS, an AI micro-coach for difficult workplace conversations.
You are grounded in the Love as a Strategy framework built on:
- Empathy & Understanding
- Respect & Psychological Safety  
- Courageous Honesty
- Shared Purpose & Trust
- Vulnerability & Openness
- Accountability with Care

Rules:
1. Always return valid JSON matching the exact schema requested
2. Be warm, direct, and practical — not preachy or generic
3. Give specific actionable coaching — not vague affirmations
4. Never replace the human conversation — only augment the human's ability to show up well
5. Tone: Like a trusted coach who believes in both love AND accountability
"""

def clarify_prompt(data):
    return f"""
The user is preparing for a difficult workplace conversation.

Scenario type: {data.get('scenario', 'General')}
Situation: {data.get('situation', '')}
Goal: {data.get('goal', '')}
Worry: {data.get('worry', '')}
How the other person might be feeling: {data.get('other_feeling', '')}

Return JSON with exactly this schema:
{{
  "reframed_intent": "A loving, clear reframe of their goal in 1-2 sentences",
  "blind_spot": "One specific empathy blind spot they might be missing",
  "emotional_context": "Brief insight into the other person's likely emotional state and what they need"
}}
"""

def draft_prompt(data):
    return f"""
The user has written a draft message/talking points for a difficult workplace conversation.

Context:
Scenario: {data.get('context', {}).get('scenario', '')}
Situation: {data.get('context', {}).get('situation', '')}
Goal: {data.get('context', {}).get('goal', '')}

Their draft:
{data.get('draft_text', '')}

Return JSON with exactly this schema:
{{
  "coached_version": "A rewritten version of their draft that keeps their core message but adds empathy, removes blame, adds psychological safety, and aligns with Love as a Strategy. Keep it natural and human — not robotic.",
  "coaching_notes": [
    {{"emoji": "💛", "note": "Short specific note about what was changed and why"}},
    {{"emoji": "🛡️", "note": "Short specific note about what was changed and why"}},
    {{"emoji": "🎯", "note": "Short specific note about what was changed and why"}}
  ]
}}
"""

def anticipate_prompt(context):
    return f"""
Based on this workplace conversation context:

Scenario: {context.get('step1', {}).get('scenario', '')}
Situation: {context.get('step1', {}).get('situation', '')}
Coached message: {context.get('draft_result', {}).get('coached_version', '')}

Simulate 3 realistic emotional reactions the other person might have.

Return JSON with exactly this schema:
{{
  "reactions": [
    {{
      "type": "Defensive",
      "emoji": "😤",
      "they_might": "What they might say or feel internally in 1-2 sentences",
      "respond_with": "How to respond with love and boundaries in 1-2 sentences"
    }},
    {{
      "type": "Anxious but Open",
      "emoji": "😟",
      "they_might": "What they might say or feel internally in 1-2 sentences",
      "respond_with": "How to respond with love and boundaries in 1-2 sentences"
    }},
    {{
      "type": "Relieved",
      "emoji": "😌",
      "they_might": "What they might say or feel internally in 1-2 sentences",
      "respond_with": "How to respond with love and boundaries in 1-2 sentences"
    }}
  ],
  "prep_summary": {{
    "intent": "One sentence summary of their loving intent",
    "key_phrase": "One powerful opening phrase they can use",
    "remember": "One thing to remember if the conversation gets hard"
  }}
}}
"""

def reflect_prompt(data):
    return f"""
The user just had a difficult workplace conversation and is reflecting on it.

Original situation: {data.get('context', {}).get('step1', {}).get('situation', '')}
What happened: {data.get('what_happened', '')}
What surprised them: {data.get('surprise', '')}
What they're proud of: {data.get('proud', '')}
What they'd do differently: {data.get('different', '')}

Return JSON with exactly this schema:
{{
  "what_you_did_with_love": "Specific affirmation of 1-2 loving behaviors they demonstrated",
  "micro_habit": "One small specific behavior change to practice next time — make it concrete and actionable",
  "love_reminder": "A short powerful principle from Love as a Strategy that applies to their situation"
}}
"""

=== requirements.txt ===
flask
openai
python-dotenv

=== .env ===
OPENAI_API_KEY=your_openai_api_key_here

=== templates/index.html ===

Build a complete single-page HTML file with ALL 4 steps. Use this exact structure:

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>EmpathyOS — Have the Conversation That Matters</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Playfair+Display:wght@600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/static/style.css">
</head>
<body>

  <!-- LANDING SECTION -->
  <section id="landing" class="section active">
    <div class="bg-circles"></div>
    <div class="landing-content">
      <div class="logo">🧠❤️ <span>EmpathyOS</span></div>
      <h1>Have the conversation<br>that <em>matters.</em></h1>
      <p class="subtitle">AI-powered coaching to help you speak with love, courage, and clarity — before and after the moments that count at work.</p>
      <div class="landing-pills">
        <span>💛 Empathy</span>
        <span>🛡️ Psychological Safety</span>
        <span>🎯 Courageous Honesty</span>
        <span>🤝 Shared Purpose</span>
      </div>
      <div class="landing-btns">
        <button class="btn-primary" onclick="showSection('step1')">Start a Conversation</button>
        <button class="btn-secondary" onclick="loadDemo()">Try Demo</button>
      </div>
    </div>
  </section>

  <!-- STEP 1: CLARIFY -->
  <section id="step1" class="section">
    <div class="container">
      <div class="progress-bar">
        <div class="progress-step active">1</div>
        <div class="progress-line"></div>
        <div class="progress-step">2</div>
        <div class="progress-line"></div>
        <div class="progress-step">3</div>
        <div class="progress-line"></div>
        <div class="progress-step">4</div>
      </div>
      <div class="step-header">
        <h2>What conversation are you<br>preparing for?</h2>
        <p>Let's start by understanding the situation and your intent.</p>
      </div>
      <div class="card">
        <div class="scenario-pills">
          <button class="pill" onclick="selectScenario(this, 'Giving tough feedback')">💬 Giving tough feedback</button>
          <button class="pill" onclick="selectScenario(this, 'Addressing a conflict')">⚡ Addressing a conflict</button>
          <button class="pill" onclick="selectScenario(this, 'Setting a boundary')">🛡️ Setting a boundary</button>
          <button class="pill" onclick="selectScenario(this, 'Asking for help')">🙏 Asking for help</button>
          <button class="pill" onclick="selectScenario(this, 'Upward feedback to manager')">📣 Upward feedback</button>
          <button class="pill" onclick="selectScenario(this, 'Custom situation')">✏️ Custom situation</button>
        </div>
        <div class="form-group">
          <label>Describe the situation briefly</label>
          <textarea id="situation" placeholder="What's happening? Who's involved? What's the context?"></textarea>
        </div>
        <div class="form-group">
          <label>What do you want to achieve?</label>
          <textarea id="goal" placeholder="What's the ideal outcome of this conversation?"></textarea>
        </div>
        <div class="form-group">
          <label>What are you most worried about?</label>
          <textarea id="worry" placeholder="What's your biggest fear going into this?"></textarea>
        </div>
        <div class="form-group">
          <label>How might the other person be feeling right now?</label>
          <textarea id="other_feeling" placeholder="Try to step into their shoes..."></textarea>
        </div>
        <button class="btn-primary full-width" onclick="submitClarify()">Get My Coaching Insight →</button>
      </div>

      <div id="clarify-result" class="result-card hidden">
        <h3>Your Coaching Insight</h3>
        <div class="insight-item">
          <span class="insight-label">💛 Your Intent (Reframed with Love)</span>
          <p id="reframed-intent"></p>
        </div>
        <div class="insight-item">
          <span class="insight-label">🔍 Blind Spot to Watch</span>
          <p id="blind-spot"></p>
        </div>
        <div class="insight-item">
          <span class="insight-label">🧠 Emotional Context</span>
          <p id="emotional-context"></p>
        </div>
        <button class="btn-primary" onclick="showSection('step2')">Continue to Draft →</button>
      </div>
    </div>
  </section>

  <!-- STEP 2: DRAFT -->
  <section id="step2" class="section">
    <div class="container">
      <div class="progress-bar">
        <div class="progress-step done">✓</div>
        <div class="progress-line active"></div>
        <div class="progress-step active">2</div>
        <div class="progress-line"></div>
        <div class="progress-step">3</div>
        <div class="progress-line"></div>
        <div class="progress-step">4</div>
      </div>
      <div class="step-header">
        <h2>What do you want to say?</h2>
        <p>Write your message or talking points. Don't worry about being perfect — that's what we're here for.</p>
      </div>
      <div class="card">
        <div class="form-group">
          <label>Your draft message or talking points</label>
          <textarea id="draft-text" rows="8" placeholder="Write what you want to say. It can be rough — just get it out..."></textarea>
        </div>
        <button class="btn-primary full-width" onclick="submitDraft()">Coach My Words →</button>
      </div>

      <div id="draft-result" class="hidden">
        <div class="comparison-grid">
          <div class="card original-card">
            <h4>📝 Your Original</h4>
            <p id="original-text"></p>
          </div>
          <div class="card coached-card">
            <h4>💛 Coached Version</h4>
            <p id="coached-text"></p>
          </div>
        </div>
        <div class="card coaching-notes">
          <h4>Coaching Notes</h4>
          <div id="notes-list"></div>
        </div>
        <button class="btn-primary" onclick="submitAnticipate()">See How They Might React →</button>
      </div>
    </div>
  </section>

  <!-- STEP 3: ANTICIPATE -->
  <section id="step3" class="section">
    <div class="container">
      <div class="progress-bar">
        <div class="progress-step done">✓</div>
        <div class="progress-line active"></div>
        <div class="progress-step done">✓</div>
        <div class="progress-line active"></div>
        <div class="progress-step active">3</div>
        <div class="progress-line"></div>
        <div class="progress-step">4</div>
      </div>
      <div class="step-header">
        <h2>How might they respond?</h2>
        <p>Understanding their possible reactions helps you stay grounded and loving — no matter what happens.</p>
      </div>
      <div id="reactions-list" class="reactions-grid"></div>
      <div id="prep-summary" class="card prep-summary hidden">
        <h4>📋 Your Conversation Prep Summary</h4>
        <div class="summary-item"><span>🎯 Intent:</span><p id="summary-intent"></p></div>
        <div class="summary-item"><span>💬 Key Phrase:</span><p id="summary-phrase"></p></div>
        <div class="summary-item"><span>🧘 Remember:</span><p id="summary-remember"></p></div>
        <button class="btn-secondary" onclick="copySummary()">📋 Copy Summary</button>
      </div>
      <button class="btn-primary" onclick="showSection('step4')" style="margin-top:24px">I Had the Conversation — Reflect Now →</button>
    </div>
  </section>

  <!-- STEP 4: REFLECT -->
  <section id="step4" class="section">
    <div class="container">
      <div class="progress-bar">
        <div class="progress-step done">✓</div>
        <div class="progress-line active"></div>
        <div class="progress-step done">✓</div>
        <div class="progress-line active"></div>
        <div class="progress-step done">✓</div>
        <div class="progress-line active"></div>
        <div class="progress-step active">4</div>
      </div>
      <div class="step-header">
        <h2>How did it go?</h2>
        <p>Reflection is where real growth happens. Take 2 minutes.</p>
      </div>
      <div class="card">
        <div class="form-group">
          <label>What happened? (briefly)</label>
          <textarea id="what-happened" placeholder="Describe how the conversation went..."></textarea>
        </div>
        <div class="form-group">
          <label>What surprised you?</label>
          <textarea id="surprise" placeholder="Was there anything unexpected?"></textarea>
        </div>
        <div class="form-group">
          <label>What are you proud of?</label>
          <textarea id="proud" placeholder="What did you do well?"></textarea>
        </div>
        <div class="form-group">
          <label>What would you do differently?</label>
          <textarea id="different" placeholder="One thing you'd change next time..."></textarea>
        </div>
        <button class="btn-primary full-width" onclick="submitReflect()">Get My Growth Insight →</button>
      </div>

      <div id="reflect-result" class="result-card hidden">
        <h3>🌱 Your Growth Insight</h3>
        <div class="insight-item">
          <span class="insight-label">💛 What You Did with Love</span>
          <p id="love-actions"></p>
        </div>
        <div class="insight-item">
          <span class="insight-label">🎯 Your Micro-Habit for Next Time</span>
          <p id="micro-habit"></p>
        </div>
        <div class="insight-item">
          <span class="insight-label">❤️ Love as a Strategy Reminder</span>
          <p id="love-reminder"></p>
        </div>
        <div class="completion-message">
          <p>Every hard conversation you have with love makes the workplace more human. <strong>Well done.</strong></p>
          <button class="btn-primary" onclick="resetApp()">Start a New Conversation</button>
        </div>
      </div>
    </div>
  </section>

  <!-- LOADING OVERLAY -->
  <div id="loading" class="loading-overlay hidden">
    <div class="heart-pulse">❤️</div>
    <p>Coaching in progress...</p>
  </div>

  <script src="/static/script.js"></script>
</body>
</html>

=== static/style.css ===

Write complete CSS for the above HTML. Requirements:
- CSS variables: --navy: #1a1a2e, --coral: #e94560, --cream: #f5f0e8, --gold: #c9a84c, --white: #ffffff
- Body background: var(--cream), font-family: Inter
- .section: display none by default, display block when .active
- .landing-content: centered, max-width 700px, margin auto, padding 80px 24px
- h1: Playfair Display, 56px, color var(--navy), line-height 1.2
- h1 em: color var(--coral), font-style italic
- .logo: flex, align-items center, gap 8px, font-weight 700, font-size 20px, color var(--navy), margin-bottom 32px
- .subtitle: font-size 18px, color #555, max-width 500px, margin 16px auto 32px
- .landing-pills: flex, flex-wrap wrap, gap 10px, justify-content center, margin-bottom 40px
- .landing-pills span: background white, border 1px solid #ddd, border-radius 20px, padding 6px 16px, font-size 14px, color #444
- .landing-btns: flex, gap 16px, justify-content center
- .btn-primary: background var(--coral), color white, border none, padding 14px 32px, border-radius 12px, font-size 16px, font-weight 600, cursor pointer, transition all 0.2s
- .btn-primary:hover: transform scale(1.02), box-shadow 0 8px 24px rgba(233,69,96,0.3)
- .btn-secondary: background white, color var(--navy), border 2px solid var(--navy), padding 14px 32px, border-radius 12px, font-size 16px, font-weight 600, cursor pointer
- .container: max-width 800px, margin 0 auto, padding 40px 24px
- .card: background white, border-radius 16px, padding 32px, box-shadow 0 4px 24px rgba(0,0,0,0.08), margin-bottom 24px
- .progress-bar: flex, align-items center, justify-content center, margin-bottom 40px, gap 0
- .progress-step: width 36px, height 36px, border-radius 50%, background #ddd, display flex, align-items center, justify-content center, font-weight 600, font-size 14px
- .progress-step.active: background var(--coral), color white
- .progress-step.done: background var(--navy), color white
- .progress-line: flex 1, height 3px, background #ddd, max-width 80px
- .progress-line.active: background var(--coral)
- .step-header: text-align center, margin-bottom 32px
- .step-header h2: Playfair Display, 36px, color var(--navy)
- .step-header p: color #666, font-size 16px, margin-top 8px
- .scenario-pills: flex, flex-wrap wrap, gap 10px, margin-bottom 24px
- .pill: background var(--cream), border 2px solid transparent, border-radius 24px, padding 8px 18px, font-size 14px, cursor pointer, transition all 0.2s
- .pill.selected: border-color var(--coral), background white, color var(--coral), font-weight 600
- .form-group: margin-bottom 20px
- .form-group label: display block, font-weight 600, color var(--navy), margin-bottom 8px, font-size 15px
- textarea: width 100%, padding 14px, border 2px solid #e8e8e8, border-radius 12px, font-family Inter, font-size 15px, resize vertical, min-height 80px, transition border 0.2s, box-sizing border-box
- textarea:focus: outline none, border-color var(--coral)
- .full-width: width 100%
- .result-card: background white, border-radius 16px, padding 32px, box-shadow 0 4px 24px rgba(0,0,0,0.08), margin-top 24px, border-left 4px solid var(--coral)
- .insight-item: margin-bottom 20px, padding-bottom 20px, border-bottom 1px solid #f0f0f0
- .insight-label: font-weight 700, color var(--navy), font-size 14px, text-transform uppercase, letter-spacing 0.5px, display block, margin-bottom 8px
- .insight-item p: color #444, line-height 1.7, margin 0
- .comparison-grid: display grid, grid-template-columns 1fr 1fr, gap 20px, margin-bottom 20px
- .original-card: border-left 4px solid #ffcccc, background #fff8f8
- .coached-card: border-left 4px solid #ccffcc, background #f8fff8
- .coaching-notes: background #fffbf0
- .note-item: display flex, align-items flex-start, gap 12px, margin-bottom 12px
- .reactions-grid: display grid, grid-template-columns repeat(auto-fit, minmax(240px, 1fr)), gap 20px, margin-bottom 24px
- .reaction-card: background white, border-radius 16px, padding 24px, box-shadow 0 4px 16px rgba(0,0,0,0.08)
- .reaction-card.defensive: border-top 4px solid #ffaa44
- .reaction-card.anxious: border-top 4px solid #4488ff
- .reaction-card.relieved: border-top 4px solid #44cc88
- .reaction-emoji: font-size 36px, margin-bottom 8px
- .prep-summary: background linear-gradient(135deg, var(--navy), #2d2d5e), color white
- .prep-summary h4: color var(--gold)
- .summary-item: margin-bottom 16px
- .summary-item span: font-weight 700, color var(--gold), font-size 13px, text-transform uppercase
- .summary-item p: color #ddd, margin 4px 0 0
- .loading-overlay: position fixed, top 0, left 0, width 100%, height 100%, background rgba(26,26,46,0.85), display flex, flex-direction column, align-items center, justify-content center, z-index 1000
- .heart-pulse: font-size 64px, animation heartbeat 1s ease-in-out infinite
- @keyframes heartbeat: 0%,100% transform scale(1); 50% transform scale(1.3)
- .loading-overlay p: color white, font-size 18px, margin-top 16px
- .hidden: display none !important
- .completion-message: text-align center, padding 24px, background var(--cream), border-radius 12px, margin-top 24px
- .bg-circles: position absolute, top 0, left 0, width 100%, height 100%, overflow hidden, z-index -1, pointer-events none
- Add 5 pseudo-element circles with CSS animation floating slowly (opacity 0.05, large border-radius, var(--coral) and var(--navy) colors)
- @media (max-width: 600px): comparison-grid grid-template-columns 1fr; h1 font-size 36px; .landing-btns flex-direction column; align-items center

=== static/script.js ===

Write complete JavaScript. Requirements:

let selectedScenario = '';
let step1Data = {};
let step2Data = {};

function showSection(id) {
  document.querySelectorAll('.section').forEach(s => s.classList.remove('active'));
  document.getElementById(id).classList.add('active');
  window.scrollTo(0, 0);
}

function showLoading() { document.getElementById('loading').classList.remove('hidden'); }
function hideLoading() { document.getElementById('loading').classList.add('hidden'); }

function selectScenario(btn, scenario) {
  document.querySelectorAll('.pill').forEach(p => p.classList.remove('selected'));
  btn.classList.add('selected');
  selectedScenario = scenario;
}

async function submitClarify() {
  const situation = document.getElementById('situation').value.trim();
  const goal = document.getElementById('goal').value.trim();
  const worry = document.getElementById('worry').value.trim();
  const other_feeling = document.getElementById('other_feeling').value.trim();
  if (!situation || !goal) { alert('Please fill in at least the situation and goal.'); return; }
  
  step1Data = { scenario: selectedScenario || 'General', situation, goal, worry, other_feeling };
  showLoading();
  
  try {
    const res = await fetch('/api/clarify', {
      method: 'POST',
      headers: {'Content-Type': 'application/json'},
      body: JSON.stringify(step1Data)
    });
    const data = await res.json();
    hideLoading();
    document.getElementById('reframed-intent').textContent = data.reframed_intent;
    document.getElementById('blind-spot').textContent = data.blind_spot;
    document.getElementById('emotional-context').textContent = data.emotional_context;
    document.getElementById('clarify-result').classList.remove('hidden');
    document.getElementById('clarify-result').scrollIntoView({behavior: 'smooth'});
  } catch(e) {
    hideLoading();
    alert('Something went wrong. Please try again.');
  }
}

async function submitDraft() {
  const draft_text = document.getElementById('draft-text').value.trim();
  if (!draft_text) { alert('Please write your draft first.'); return; }
  
  step2Data = { draft_text };
  showLoading();
  
  try {
    const res = await fetch('/api/draft', {
      method: 'POST',
      headers: {'Content-Type': 'application/json'},
      body: JSON.stringify(step2Data)
    });
    const data = await res.json();
    hideLoading();
    document.getElementById('original-text').textContent = draft_text;
    document.getElementById('coached-text').textContent = data.coached_version;
    const notesList = document.getElementById('notes-list');
    notesList.innerHTML = data.coaching_notes.map(n => 
      `<div class="note-item"><span style="font-size:20px">${n.emoji}</span><p>${n.note}</p></div>`
    ).join('');
    document.getElementById('draft-result').classList.remove('hidden');
    document.getElementById('draft-result').scrollIntoView({behavior: 'smooth'});
  } catch(e) {
    hideLoading();
    alert('Something went wrong. Please try again.');
  }
}

async function submitAnticipate() {
  showSection('step3');
  showLoading();
  
  try {
    const res = await fetch('/api/anticipate', {
      method: 'POST',
      headers: {'Content-Type': 'application/json'},
      body: JSON.stringify({})
    });
    const data = await res.json();
    hideLoading();
    
    const colorMap = { 0: 'defensive', 1: 'anxious', 2: 'relieved' };
    const reactionsList = document.getElementById('reactions-list');
    reactionsList.innerHTML = data.reactions.map((r, i) => `
      <div class="reaction-card ${colorMap[i]}">
        <div class="reaction-emoji">${r.emoji}</div>
        <h4>${r.type}</h4>
        <p><strong>They might feel:</strong> ${r.they_might}</p>
        <p><strong>Respond with love:</strong> ${r.respond_with}</p>
      </div>
    `).join('');
    
    const ps = data.prep_summary;
    document.getElementById('summary-intent').textContent = ps.intent;
    document.getElementById('summary-phrase').textContent = ps.key_phrase;
    document.getElementById('summary-remember').textContent = ps.remember;
    document.getElementById('prep-summary').classList.remove('hidden');
  } catch(e) {
    hideLoading();
    alert('Something went wrong. Please try again.');
  }
}

function copySummary() {
  const intent = document.getElementById('summary-intent').textContent;
  const phrase = document.getElementById('summary-phrase').textContent;
  const remember = document.getElementById('summary-remember').textContent;
  const text = `EmpathyOS Prep Summary\n\nIntent: ${intent}\nKey Phrase: "${phrase}"\nRemember: ${remember}`;
  navigator.clipboard.writeText(text).then(() => alert('Summary copied to clipboard!'));
}

async function submitReflect() {
  const what_happened = document.getElementById('what-happened').value.trim();
  const surprise = document.getElementById('surprise').value.trim();
  const proud = document.getElementById('proud').value.trim();
  const different = document.getElementById('different').value.trim();
  if (!what_happened) { alert('Please describe what happened.'); return; }
  
  showLoading();
  
  try {
    const res = await fetch('/api/reflect', {
      method: 'POST',
      headers: {'Content-Type': 'application/json'},
      body: JSON.stringify({ what_happened, surprise, proud, different })
    });
    const data = await res.json();
    hideLoading();
    document.getElementById('love-actions').textContent = data.what_you_did_with_love;
    document.getElementById('micro-habit').textContent = data.micro_habit;
    document.getElementById('love-reminder').textContent = data.love_reminder;
    document.getElementById('reflect-result').classList.remove('hidden');
    document.getElementById('reflect-result').scrollIntoView({behavior: 'smooth'});
    launchConfetti();
  } catch(e) {
    hideLoading();
    alert('Something went wrong. Please try again.');
  }
}

function launchConfetti() {
  const canvas = document.createElement('canvas');
  canvas.style.cssText = 'position:fixed;top:0;left:0;width:100%;height:100%;pointer-events:none;z-index:9999';
  document.body.appendChild(canvas);
  const ctx = canvas.getContext('2d');
  canvas.width = window.innerWidth;
  canvas.height = window.innerHeight;
  const pieces = Array.from({length: 80}, () => ({
    x: Math.random() * canvas.width,
    y: Math.random() * canvas.height - canvas.height,
    r: Math.random() * 8 + 4,
    color: ['#e94560','#c9a84c','#1a1a2e','#44cc88','#4488ff'][Math.floor(Math.random()*5)],
    speed: Math.random() * 3 + 2,
    angle: Math.random() * 360
  }));
  let frame = 0;
  function draw() {
    ctx.clearRect(0, 0, canvas.width, canvas.height);
    pieces.forEach(p => {
      ctx.fillStyle = p.color;
      ctx.beginPath();
      ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2);
      ctx.fill();
      p.y += p.speed;
      p.x += Math.sin(p.angle * Math.PI / 180) * 1.5;
      p.angle += 2;
    });
    frame++;
    if (frame < 120) requestAnimationFrame(draw);
    else canvas.remove();
  }
  draw();
}

function loadDemo() {
  showSection('step1');
  setTimeout(() => {
    document.querySelectorAll('.pill').forEach(p => {
      if (p.textContent.includes('tough feedback')) {
        selectScenario(p, 'Giving tough feedback');
      }
    });
    document.getElementById('situation').value = "My teammate has been missing deadlines and it's affecting the whole team. I need to talk to them but I don't want to damage our relationship.";
    document.getElementById('goal').value = "I want them to understand the impact and commit to a change, while keeping our trust intact.";
    document.getElementById('worry').value = "They'll get defensive or feel attacked and shut down.";
    document.getElementById('other_feeling').value = "They might be overwhelmed — they mentioned personal stress last week.";
  }, 300);
}

function resetApp() {
  selectedScenario = '';
  step1Data = {};
  step2Data = {};
  document.getElementById('situation').value = '';
  document.getElementById('goal').value = '';
  document.getElementById('worry').value = '';
  document.getElementById('other_feeling').value = '';
  document.getElementById('draft-text').value = '';
  document.getElementById('clarify-result').classList.add('hidden');
  document.getElementById('draft-result').classList.add('hidden');
  document.getElementById('reflect-result').classList.add('hidden');
  document.querySelectorAll('.pill').forEach(p => p.classList.remove('selected'));
  showSection('landing');
}

=== IMPORTANT FINAL INSTRUCTIONS ===
1. Make sure app.py runs on host='0.0.0.0', port=5000
2. Make sure the .env file has OPENAI_API_KEY placeholder
3. Make sure all 4 API routes return valid JSON
4. Make sure Flask serves static files correctly (app = Flask(__name__))
5. Test that fetch() calls in script.js match the exact Flask route paths
6. The app must work end-to-end: Landing → Step1 → Step2 → Step3 → Step4 → Done

After pasting this prompt in Replit:
Add your OpenAI API key in the .env file → OPENAI_API_KEY=sk-...
In Replit Secrets (🔒 icon), also add OPENAI_API_KEY as a secret
Click Run — it should work fully

Want me to also give you the pitch script for Feb 28 while you're building this?