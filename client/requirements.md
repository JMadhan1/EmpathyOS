## Packages
framer-motion | Page transitions and animated reveals for a premium feel
react-confetti | Celebration animation upon completing the reflection step
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging tailwind classes safely

## Notes
- Assumes `@shared/schema` and `@shared/routes` are properly configured as per the provided manifest.
- Uses standard REST JSON payloads for AI endpoints.
- Uses space-separated HSL colors for Tailwind styling.
